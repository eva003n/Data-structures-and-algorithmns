#include <stdio.h>
#include <stdlib.h>

#include "hash_table.h"

static ht_item *ht_new_item(const char *, const char *);
int main()
{
    int capacity =

        return 0;
}
static ht_item *ht_new_item(const char *, const char *)
{
    ht_item *items = malloc()
}
